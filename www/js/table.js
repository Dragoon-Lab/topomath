define([
	"dojo/_base/declare"
], function(declare){
	return declare(calculations, {
		/**
		* for table rendering stuff. Yet to see and understand the code.
		* This part should be very reusable as the functionality is still the same.
		**/
	});
});
